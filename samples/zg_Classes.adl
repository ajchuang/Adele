Class HorizontalLine {
	int length;

	HorizontalLine(Graph g, int length) {
		this.length = length;

		line = g;
		(length-1) times {
			line = line - g;
		}

		return line;
	}
}

# Excuese me, I didn't finsh this definition, it's really 
# hard to describe a graph in my version of Adele T^T

Class Rectangular {
	Rectangular(Graph[] border, Graph[] corner){
		rect = corners[0] - border[0] - corner[1];
		rect = rect | border[1] @ rect.end;
		rect = s...
	}

	fill(Graph pattern) {

	}
}